---
title: "#29 ✅ #T-C-115 Step 3 — push·CI 통과 + 사이트 검증, callout 다크/spacing 영구 fix 묶음"
date: 2026-05-18
tags:
  - worklog
---
## #29 ✅ #T-C-115 Step 3 — push·CI 통과 + 사이트 검증, callout 다크/spacing 영구 fix 묶음

- 일자: 2026-05-18
- 작업자: 최대리
- 지시: 김부장 (워크로그 #28 다음 액션)

> "Step 3 push·CI·curl·브라우저 검증 + callout 다크 매핑 한 사이클로."

### 시작
#28에서 코드 픽스 3건(contentIndex emitter + config + Explorer filterFn)·로컬 빌드 검증까지 끝낸 상태. 본 사이클은 commit·push·라이브 검증 + Eric 추가 항목(callout 다크 토큰·h2/callout spacing)을 별 commit으로 묶음. 김부장 안건의 "단위 commit 분리 권장" 룰 그대로.

### 실행
1. **commit 1 (`30b87cc`)** — 누출 차단 코드 3건 묶음. `quartz.config.ts` + `quartz.layout.ts` + `contentIndex.tsx`. 메시지에 로컬 검증 결과(`worklogs URL=0` 4종 산출물) 인용. push 즉시 OK.
2. **callout 본문 원인 분석** — `base.scss`의 body color = `var(--darkgray)` → 다크에서 `--ec-dark-text-muted`(#a8a0b8)로 매핑. 본문 `article`은 `.center article` 룰로 `var(--dark)` 강제, 그러나 `.callout > .callout-content`는 grid container라 부모 article 룰 안 잡힘 → muted 상속. 원인 명확.
3. **commit 2 (`1d6f366`)** — `quartz/styles/custom.scss` 끝에 typography 묶음 추가:
   - `article h2 { margin-top: 2.5rem; margin-bottom: 0.75rem; }` (섹션 분리 영구 fix)
   - `.callout { margin: 1.5rem 위·아래; }`
   - `.callout > .callout-content`·`p`·`li` color: `var(--dark)` 명시
4. 로컬 `rm -rf public .quartz-cache && npx quartz build` — 89 input → 202 emitted, 4s. 회귀 0.
5. push → CI 빌드 완료(`last-modified: 2026-05-18 00:25:10 GMT`).
6. **라이브 검증 매트릭스:**

| 검증 항목 | 결과 |
|---|---|
| 메인 `worklogs/[a-z]/[0-9]+` grep | **0건** ✅ |
| `static/contentIndex.json` worklogs 키 | **0건** ✅ |
| `static/contentIndex.json` diary 키 | **7건** ✅ (외부 노출 정상) |
| `/worklogs/c/1` staticrypt 마커 | **70건** ✅ (암호화 보호 유지) |
| 메인·`/diary/a/15` HTTP | **200** ✅ |

### 결과
- 커밋 2건(`30b87cc` 누출 차단 / `1d6f366` typography) 분리 push ✅
- 그래프·검색·sitemap·RSS 4종 worklogs 메타데이터 누출 **0** ✅
- diary 정상 노출 7건 ✅ — 외부 = diary만, worklogs는 페이지만 staticrypt
- callout 본문 다크 토큰 + h2/callout spacing 영구 fix ✅
- 큐 `#T-C-115` ✅ 마킹

### 배운 점 (← dream 입력)
- **패턴/원칙:** SSG 보호 게이트 결재 시 체크리스트 = (1) HTML 본문, (2) 검색 인덱스, (3) sitemap, (4) RSS, (5) 그래프 데이터, (6) Explorer 사이드바, (7) oG 이미지. emitter에서 한 곳 patch면 1~4 동시 정리 (#28 학습), Component쪽은 별 게이트(#28 filterFn). 본 사이클은 그 두 갈래 라이브 검증으로 닫힘.
- **새로 통한 것:** callout 본문 컬러 muted 떠 보임 원인 = grid container가 부모 article의 강제 룰 차단. `.callout > .callout-content { color: var(--dark) }` 한 줄로 일관. CSS cascade에서 grid wrapper가 색 상속의 단절점이 된다는 미세한 결.
- **막힘 회복:** 백그라운드 `sleep 90 && curl` 출력이 0 bytes로 안 떨어져 대기 길어졌는데, 그냥 직접 curl로 재실행해도 결과 같으니 깔끔. polling 대신 즉시 재호출.

### 자체 평가
⭐⭐⭐⭐⭐ — 2 commit 깔끔 분리, 라이브 5종 검증 통과, callout 원인 분석 → 한 줄 픽스. #27~#29 3 사이클로 #T-C-115 닫음.

### 다음 액션
- [ ] #T-C-116 (날짜 포맷 `2026.05.15` + 폴더 카운트 메시지 제거) — 큐 다음 미처리. 로컬 grep: `May 15, 2026` + `under this folder` 둘 다 존재 확인. quartz.config.ts `locale`/`dateFormat` 또는 ContentMeta 컴포넌트 + i18n `itemsUnderFolder` 키 처리.

### 메타
- 공개: public
- 태그: #omc #meta #publish #quartz #css #callout #typography #픽스
- 링크: [[#27]] [[#28]]
